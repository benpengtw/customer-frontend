version: '3.0'

services:
  customer-frontend:
    image: nginx
    ports:
      - 83:80
    restart: unless-stopped
    volumes:
      - ./dist:/usr/share/nginx/html:ro
      - ./nginx.conf:/etc/nginx/conf.d/default.conf
